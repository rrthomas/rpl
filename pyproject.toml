[build-system]
requires = ["argparse-manpage[setuptools] @ git+https://github.com/rrthomas/argparse-manpage.git"]

[tool.mypy]
exclude = [
    '^build_manpages',
    '^dist',
    '^build',
    '^tests-output',
    'setup.py',
]
strict = true
